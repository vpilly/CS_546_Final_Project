<div class="nav">
    <a class="login" href="/artists">artists</a>
    <a class="login" href="/concerts">concerts</a>
    <a class="login" href="/logout">logout</a>
</div>
<h1>{{title}}</h1>
    <h2>{{concert.title}}</h2>
        <main class="concert">
            <dl>
                <dt>Title</dt>
                <dd>{{concert.title}}</dd>
                <dt>Artists</dt>
                <dd>{{concert.artists}}</dd>
                <dt>Date</dt>
                <dd>{{concert.date}}</dd>
                <dt>Time</dt>
                <dd>{{concert.time}}</dd>
                <dt>Address</dt>
                <dd>{{concert.address}}</dd>
                <dt>Zip Code</dt>
                <dd>{{concert.zipcode}}</dd>
                <dt>Venue</dt>
                <dd>{{concert.venue}}</dd>
                <dt>Genre</dt>
                <dd>{{concert.genre}}</dd>
                <dt>Description</dt>
                <dd>{{concert.description}}</dd>
                <dt>Ticket Price</dt>
                <dd>{{concert.ticketPrice}}</dd>
            </dl> 
        </main>

